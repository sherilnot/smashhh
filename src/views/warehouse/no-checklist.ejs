<%- include('../partials/header', { title: 'No Checklist - Burger Store', role: 'warehouse_manager', active: 'checklist' }) %>

  <h1><span class="emoji">📦</span>Inventory Checklist</h1>

  <form class="date-form" method="GET" action="/warehouse/checklist">
    <label>Date: <input type="date" name="date" value="<%= date.getFullYear() %>-<%= String(date.getMonth()+1).padStart(2,'0') %>-<%= String(date.getDate()).padStart(2,'0') %>" /></label>
    <button type="submit">Load</button>
  </form>
  <div class="info">No deliveries expected for <%= date.toLocaleDateString() %>.</div>

<%- include('../partials/footer') %>
